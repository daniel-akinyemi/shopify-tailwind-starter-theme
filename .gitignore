# Dependencies
node_modules/

# NOTE: assets/application.css (the compiled Tailwind output) is intentionally
# committed. This theme deploys via Shopify's GitHub integration, which has no
# build step, so the compiled stylesheet must live in the repo. Rebuild it with
# `npm run build` and commit the result whenever styles change.

# Shopify CLI
.shopify/
*.log

# OS
.DS_Store

# Claude Code
.claude/
